@NonCPS
def log(name) {
    echo name
}

pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                script {
                    for(i in 1..5) {
                        echo "#"+i
                    }
                }
                log "Build"
                echo 'Hello World'
            }
        }
    }
}
